# SMTP

SMTP is used only for notifications and the daily report. Outreach emails are created as Gmail drafts and are not sent automatically. Follow the [SMTP section of the credential guide](credentials.md#smtp-for-notifications-and-daily-reports) to create and attach the credential.

Keep approval notifications separate from outreach sending. Run each workflow manually with sample input before activating schedules.
